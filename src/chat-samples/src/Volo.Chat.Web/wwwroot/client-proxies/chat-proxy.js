/* This file is automatically generated by ABP framework to use MVC Controllers from javascript. */


// module chat

(function(){

  // controller volo.chat.users.contact

  (function(){

    abp.utils.createNamespace(window, 'volo.chat.users.contact');

    volo.chat.users.contact.getContacts = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/chat/contact/contacts' + abp.utils.buildQueryString([{ name: 'filter', value: input.filter }, { name: 'includeOtherContacts', value: input.includeOtherContacts }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

    volo.chat.users.contact.getTotalUnreadMessageCount = function(ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/chat/contact/total-unread-message-count',
        type: 'GET'
      }, ajaxParams));
    };

  })();

  // controller volo.chat.settings.settings

  (function(){

    abp.utils.createNamespace(window, 'volo.chat.settings.settings');

    volo.chat.settings.settings.setSendOnEnterSetting = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/chat/settings/send-on-enter',
        type: 'POST',
        dataType: null,
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    volo.chat.settings.settings.get = function(ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/chat/settings',
        type: 'GET'
      }, ajaxParams));
    };

    volo.chat.settings.settings.update = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/chat/settings',
        type: 'PUT',
        dataType: null,
        data: JSON.stringify(input)
      }, ajaxParams));
    };

  })();

  // controller volo.chat.conversations.conversation

  (function(){

    abp.utils.createNamespace(window, 'volo.chat.conversations.conversation');

    volo.chat.conversations.conversation.sendMessage = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/chat/conversation/send-message',
        type: 'POST',
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    volo.chat.conversations.conversation.deleteMessage = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/chat/conversation/delete-message' + abp.utils.buildQueryString([{ name: 'targetUserId', value: input.targetUserId }, { name: 'messageId', value: input.messageId }]) + '',
        type: 'DELETE',
        dataType: null
      }, ajaxParams));
    };

    volo.chat.conversations.conversation.getConversation = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/chat/conversation/conversation' + abp.utils.buildQueryString([{ name: 'targetUserId', value: input.targetUserId }, { name: 'skipCount', value: input.skipCount }, { name: 'maxResultCount', value: input.maxResultCount }]) + '',
        type: 'GET'
      }, ajaxParams));
    };

    volo.chat.conversations.conversation.markConversationAsRead = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/chat/conversation/mark-conversation-as-read',
        type: 'POST',
        dataType: null,
        data: JSON.stringify(input)
      }, ajaxParams));
    };

    volo.chat.conversations.conversation.deleteConversation = function(input, ajaxParams) {
      return abp.ajax($.extend(true, {
        url: abp.appPath + 'api/chat/conversation/delete-conversation' + abp.utils.buildQueryString([{ name: 'targetUserId', value: input.targetUserId }]) + '',
        type: 'DELETE',
        dataType: null
      }, ajaxParams));
    };

  })();

})();


